#!/usr/bin/env bash
set -euo pipefail

client="${1:-}"
payload="${2:-}"

if [[ -z "$payload" && "$client" == \{* ]]; then
  payload="$client"
  client="${AGENT_SIGNALS_DEFAULT_CLIENT:-Codex}"
fi

exec agent-signal notify --client "$client" "$payload"
